<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

<style>
    * { font-family: 'Poppins', sans-serif; color: #1a1a1a; }
    body { background-color: #f8f8f6; margin: 0; padding: 0; display: flex; min-height: 100vh; }

    .sidebar {
        width: 260px;
        min-height: 100vh;
        background-color: #BDD7E7;
        display: flex;
        flex-direction: column;
        padding: 24px 16px;
        position: fixed;
        top: 0; left: 0;
        box-shadow: 2px 0 8px rgba(0,0,0,0.08);
    }
    .sidebar-logo {
        text-align: center;
        margin-bottom: 32px;
    }
    .sidebar-logo img { height: 80px; object-fit: contain; }
    .sidebar-nav {
        display: flex;
        flex-direction: column;
        gap: 10px;
        flex: 1;
    }
    .btn-sidebar {
        background-color: white;
        color: #1a1a1a;
        border: none;
        border-radius: 8px;
        padding: 12px 16px;
        font-weight: 600;
        text-align: left;
        text-decoration: none;
        display: block;
        transition: background-color 0.2s ease, transform 0.1s ease;
    }
    .btn-sidebar:hover {
        background-color: #e8f0f5;
        transform: translateX(3px);
        color: #1a1a1a;
    }
    .btn-sidebar.ativo { background-color: #2E6A8E; color: white; }
    .btn-sidebar.ativo:hover { color: white; }
    .sidebar-divider {
        border: none;
        border-top: 1px solid rgba(0,0,0,0.15);
        margin: 16px 0;
    }
    .sidebar-footer { display: flex; flex-direction: column; gap: 10px; }
    .btn-sidebar-link {
        background: none;
        border: none;
        padding: 8px 4px;
        text-align: left;
        color: #1a1a1a;
        font-size: 0.95rem;
        text-decoration: none;
        display: block;
        transition: color 0.2s ease;
        font-weight: 500;
    }
    .btn-sidebar-link:hover { color: #2E6A8E; }
    .btn-sair { color: #F24822; }
    .btn-sair:hover { color: #c03518; }

    .main-content {
        margin-left: 260px;
        padding: 48px;
        flex: 1;
        width: calc(100% - 260px);
    }
    .main-titulo {
        font-size: 1.8rem;
        font-weight: 700;
        margin-bottom: 8px;
    }
    .main-subtitulo {
        color: #777;
        font-size: 0.95rem;
        margin-bottom: 40px;
        font-weight: 400;
    }

    /* grid de cards */
    .cards-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
        gap: 24px;
        max-width: 900px;
    }
    .card-relatorio {
        background: white;
        border-radius: 16px;
        padding: 28px 24px;
        box-shadow: 0 2px 12px rgba(0,0,0,0.06);
        text-decoration: none;
        display: block;
        border-top: 4px solid transparent;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
        position: relative;
        overflow: hidden;
    }
    .card-relatorio:hover {
        transform: translateY(-4px);
        box-shadow: 0 8px 24px rgba(0,0,0,0.1);
    }
    .card-relatorio.azul { border-top-color: #2E6A8E; }
    .card-relatorio.verde { border-top-color: #14ae5c; }
    .card-relatorio.laranja { border-top-color: #F24822; }
    .card-relatorio.cinza { border-top-color: #6c757d; }

    .card-titulo {
        font-weight: 700;
        font-size: 1rem;
        color: #1a1a1a;
        margin-bottom: 8px;
    }
    .card-descricao {
        font-size: 0.85rem;
        color: #888;
        font-weight: 400;
        line-height: 1.5;
    }
    .card-seta {
        position: absolute;
        bottom: 20px;
        right: 20px;
        font-size: 1.1rem;
        color: #ccc;
        transition: color 0.2s ease, right 0.2s ease;
    }
    .card-relatorio:hover .card-seta {
        color: #2E6A8E;
        right: 16px;
    }
</style>

<div class="sidebar">
    <div class="sidebar-logo">
        <img src="/images/logo.jpg" alt="Logo" onerror="this.style.display='none'" />
    </div>
    <nav class="sidebar-nav">
        <a href="/produto/index" class="btn-sidebar">Estoque</a>
        <a href="/produto/create" class="btn-sidebar">Novo Produto</a>
        <a href="/produto/movimentar" class="btn-sidebar">Movimentar Estoque</a>
        <a href="/produto/relatorios" class="btn-sidebar ativo">Relatórios</a>
    </nav>
    <hr class="sidebar-divider" />
    <div class="sidebar-footer">
        <a href="/Usuario/Index" class="btn-sidebar-link">Gerenciar Usuários</a>
        <a href="/Usuario/Logout" class="btn-sidebar-link btn-sair">Sair</a>
    </div>
</div>

<div class="main-content">
    <div class="main-titulo">Relatórios</div>
    <div class="main-subtitulo">Selecione um relatório para visualizar</div>

    <div class="cards-grid">

        <a href="/produto/relatorioHistoricoMovimentacoes" class="card-relatorio azul">
            <div class="card-titulo">Histórico de Movimentações</div>
            <div class="card-descricao">Visualize todas as entradas e saídas registradas no sistema.</div>
        </a>

        <a href="/produto/relatorioEstoqueBaixo" class="card-relatorio laranja">
            <div class="card-titulo">Estoque Baixo</div>
            <div class="card-descricao">Produtos com tamanhos com 2 unidades ou menos — hora de repor.</div>
        </a>

        <a href="/produto/relatorioHistoricoPorProduto" class="card-relatorio cinza">
            <div class="card-titulo">Histórico por Produto</div>
            <div class="card-descricao">Selecione um produto e veja todo o histórico de movimentações dele.</div>
        </a>

    </div>
</div>